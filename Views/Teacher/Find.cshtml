@model FakeBK.Models.Teacher
@{
    ViewData["Title"] = "Find Teacher";
}

<h2>Find Student</h2>

<form asp-action="Find" method="get">
    <div class="form-group">
        <label for="StudentId">Enter Teacher ID:</label>
        <input type="number" id="TeacherID" name="Id" class="form-control" required />
    </div>
    <button type="submit" class="btn btn-primary mt-2">Search</button>
    <div class="col-6 col-md-3">
        <a asp-controller="Teacher" asp-action="Index" class="btn btn-secondary form-control">
            Go back
        </a>
    </div>
</form>

@if (ViewBag.Message != null)
{
    <div class="alert alert-info mt-3">
        @ViewBag.Message
    </div>
}

@if (Model != null)
{
    <h3>Teacher Details</h3>
    <ul>
        <li><strong>ID:</strong> @Model.Id</li>
        <li><strong>Personal ID:</strong> @Model.OrgpId</li>
        <li><strong>DepartmentID:</strong> @Model.DeptId</li>
       
    </ul>
}

